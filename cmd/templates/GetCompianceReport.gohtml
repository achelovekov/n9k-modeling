<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Input Type Submit</title>
    <link rel="stylesheet" type="text/css" href="main.css">
</head>
<body>
    {{range $i, $v := .}}
    <h4>{{$v.DeviceName}}</h4>
    <table>
        <tr>
            <th>Key</th>
            <th>ToChange</th>
            <th>ToAdd</th>
        </tr>
        {{range $i, $v := $v.DiffData}}
        <tr>
            <th>{{$v.Key}}</th>
            <td>
                {{range $k, $v := $v.ToChange}}
                <code>{{$k}}: {{$v}}</code><br>
                {{end}}
            </td>
            <td>
                {{range $k, $v := $v.ToAdd}}
                <code>{{$k}}: {{$v}}</code><br>
                {{end}}
            </td>
        </tr>
        {{end}}
    </table>
    {{end}}
</body>
</html>